# V7 · 成片质量打磨（第二轮）

> **历史归档**：本文仅作里程碑记录。现行以 [`DEV_LOCK.md`](DEV_LOCK.md) / [`PRODUCT_PLAN.md`](PRODUCT_PLAN.md) / [`DEVELOPMENT_STANDARDS.md`](DEVELOPMENT_STANDARDS.md) 为准；冲突句作废。

目标：在 Q1–Q3 基础上继续压「质检漏网、开场像结尾、同任务刷同款」。

## 范围

| ID | 改动 |
|----|------|
| Q4 | 质检加强：黑场占比、近静音、响度过低 → `failed` |
| Q5 | 槽位角色：hook / body / cta 使用差异化语义查询 |
| Q8 | 同 job 多样性：近期标题/钩子冷却；同任务内已用素材加权避开 |

## 验收（本机）

- dry-run 警告含槽位角色差异化取片路径
- QC metrics 含 `black_ratio`；无音轨时不因静音硬失败
- 同 job 连续出片时 `job_used_assets` 降权已用素材

## 非目标（当时）

- 发布、数字人  
- Logo 片头（后归 Sprint C）  

## 已过时（勿再引用）

- ~~成片仍 `-an`~~ → **已废**：Sprint A 起默认有 BGM 音轨（见 V8_QUALITY / DEV_LOCK）。
