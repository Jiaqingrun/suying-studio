> **历史归档**：本文仅作里程碑记录。现行以 [`DEV_LOCK.md`](DEV_LOCK.md) / [`PRODUCT_PLAN.md`](PRODUCT_PLAN.md) / [`DEVELOPMENT_STANDARDS.md`](DEVELOPMENT_STANDARDS.md) 为准；冲突句作废。

# V4 · 视觉描述增强（继续强化 Q2）

目标：用本机视觉模型给 cliplet 写真实画面描述，提升主题语义取片精度。

## 范围

1. 抽帧 → Ollama 视觉模型（优先 `gemma4`）生成中文短描述  
2. 失败则回退启发式描述  
3. 全库 cliplet 重描述 + embedding 重索引  
4. API：`POST /index/captions` 可强制重跑  

## 非目标

- ~~Proxy 管线~~ → 见 `docs/V5.md`；发布、数字人
