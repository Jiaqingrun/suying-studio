> **历史归档**：本文仅作里程碑记录。现行以 [`DEV_LOCK.md`](DEV_LOCK.md) / [`PRODUCT_PLAN.md`](PRODUCT_PLAN.md) / [`DEVELOPMENT_STANDARDS.md`](DEVELOPMENT_STANDARDS.md) 为准；冲突句作废。

# V5 · Proxy 代理媒体

目标：入库生成低码率代理片，供场景检测 / 视觉抽帧使用；成片渲染仍用规范化原片时间码。

## 范围

1. 规范化后额外生成 `proxy.mp4`（约 720p / 15fps / 高压缩）  
2. `Asset.proxy_path` 落库  
3. cliplet 场景检测、视觉抽帧优先读 proxy  
4. `POST /assets/proxies/backfill` 为存量补生成  

## 验收（本机）

- 存量 18 条素材 backfill：`created=18 failed=0`  
- `analysis_video_path` 全部指向 `proxy.mp4`；渲染仍用 `normalized.mp4`  
- 成片任务仍可完成（job #11 completed）

## 非目标

- 多客户隔离、发布、数字人
